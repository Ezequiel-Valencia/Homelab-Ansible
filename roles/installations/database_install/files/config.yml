http:
 #region routers 
  routers:        
    qdrant:
      entryPoints:
        - "https"
      rule: "Host(`qdrant-ui.database.homelab.ezequielvalencia.com`)"
      middlewares:
        - secured
      tls: {}
      service: qdrant

    pgadmin:
      entryPoints:
        - "https"
      rule: "Host(`pgadmin.database.homelab.ezequielvalencia.com`)"
      middlewares:
        - secured
      tls: {}
      service: pgadmin

  services:
    qdrant:
      loadBalancer:
        servers:
          - url: "http://qdrant:6333"
    pgadmin:
      loadBalancer:
        servers:
          - url: "http://pgadmin:80"

#endregion
  middlewares:
    https-redirectscheme:
      redirectScheme:
        scheme: https
        permanent: true

    default-headers:
      headers:
        frameDeny: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 15552000
        customFrameOptionsValue: SAMEORIGIN
        customRequestHeaders:
          X-Forwarded-Proto: https

    default-whitelist:
      ipAllowList:
        sourceRange:
        - "10.0.0.0/8"
        # - "192.168.0.0/16"
        # - "172.16.0.0/12"

    secured:
      chain:
        middlewares:
        - default-whitelist
        - default-headers
        - https-redirectscheme

tpc:
  routers:
    postgres:
      entryPoints:
        - pg
      rule: "Host(`postgres.database.homelab.ezequielvalencia.com`)"
      middlewares:
        - secured
    qdrant:
      entryPoints:
        - qdrant
      rule: "Host(`qdrant.database.homelab.ezequielvalencia.com`)"
      middlewares:
        - secured

  services:
    # Tools
    postgres:
      loadBalancer:
        servers:
          - url: "postgres:5432"
    qdrant:
      loadBalancer:
        servers:
          - url: "qdrant:6334"
  
  middlewares:
    default-whitelist:
      ipAllowList:
        sourceRange:
        - "10.0.0.0/8"
        # - "192.168.0.0/16"
        # - "172.16.0.0/12"

    secured:
      chain:
        middlewares:
        - default-whitelist


